# lefthook — git hooks for the mokout repo. Run `npx lefthook install` once.
pre-commit:
  parallel: true
  commands:
    biome:
      glob: "*.{js,ts,json}"
      run: npx biome check --write {staged_files}
      stage_fixed: true
    typecheck:
      run: npm run typecheck
