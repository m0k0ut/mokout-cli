# lefthook — git hooks for the mokout repo. Run `bunx lefthook install` once.
pre-commit:
  parallel: true
  commands:
    biome:
      glob: "*.{js,ts,json}"
      run: bunx biome check --write {staged_files}
      stage_fixed: true
    typecheck:
      run: bun run typecheck
